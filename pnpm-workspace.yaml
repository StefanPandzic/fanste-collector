packages:
  - apps/*
  - packages/*

# Dependencies allowed to run install scripts (pnpm blocks them by default).
allowBuilds:
  # Its postinstall only downloads a native binary when the optional platform package is missing.
  unrs-resolver: false
